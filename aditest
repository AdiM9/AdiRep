
const { Builder, By, until } = require('selenium-webdriver');

async function adiTest() {

const driver = await new Builder().forBrowser('chrome').build();
// implicit wait-usually not a good practice to mix with explicit waits
await driver.manage().setTimeouts({implicit: 5000});

try {

await driver.get('https://example.com');

const usernameInput = await driver.wait(locateElement(By.id('username')), 6000);
await driver.wait(elementIsVisible(usernameInput), 6000);

//if I need to clear the field before typing
await usernameInput.clear();
await usernameInput.sendKeys('adimacic123');

const passwordInput = await driver.wait(elementLocated(By.id('pwd-field')), 7000);
await pdriver.wait(elementIsVisible(passwordInput), 7000);
await passwordInput.sendKeys('123was');

const loginbtn = await driver.wait(locateElement(By.id('loggobth')), 6000);
await driver.wait(elementIsVisible('loginbtn'), 6000);
await loginbtn.click();

//if I need to verify successfull login
await driver.wait(until.urlContains('/dashboard'), 6000);

} finally {
    await driver.quit();
}

}
adiTest().catch(console.error);